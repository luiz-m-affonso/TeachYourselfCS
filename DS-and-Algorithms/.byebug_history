exit
end
  strings.group_by { |s| s.split(/\s+/).first }
def group_title(strings)
end
  strings.group_by { |s| s.split(/\s+/).first }
def group_title(strings)
end
  strings.group_by { |s| s.split(/\s+/).first }
def group_title(strings)
require 'byebug'
exit
